# codex 扒的真人评测素材（不是成稿）

每个门类一份，codex 用网络搜索从真人评测/食记里提取的感官描写，大量【待舌头】是它诚实的地方——她吃到哪个门类，合她的舌头时从这里捡真料（Taquitos 那类实吃记录、食記、盲测），不要整段搬。品牌名只在这里出现，进盒的文本不许有。

生成方式：`drafts/codex_batch/run.sh`（codex exec，read-only 沙箱，走订阅零成本），简报模板见 `drafts/codex_batch/brief_<门类>.md`（drafts 不进 git，模板逻辑在 09-04 的开发日志里）。
